{
  "name": "schedule-widget",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "craco start",
    "build": "BUILD_PATH=../../dist/apps/schedule-widget craco build",
    "test": "craco test",
    "eject": "react-scripts eject",
    "serve": "npm start"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
